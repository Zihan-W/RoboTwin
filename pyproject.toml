[build-system]
requires = ["flit_core >=3.7,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "script"
version = "0.1.0"
description = "Diffusion policy for RoboTwin"
requires-python = ">=3.8"